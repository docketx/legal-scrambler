// Ceilings the frontier orchestrator's public-law tools accept. Same values as DocketRouter's API schemas so a
// tool call shaped here is a tool call shaped there.
export const MAX_K = 20;
export const MAX_CITATIONS = 100;
